apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: vaults.helm.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: helm.hops.ops.com.ai/v1alpha1
    kind: Vault
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-helm-vaultrender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
